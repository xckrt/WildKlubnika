<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".FragmentSellerDashboard">

    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Продавец"
        android:textSize="24sp"
        android:layout_marginTop="20dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Кнопка для добавления товара -->
    <Button
        android:id="@+id/addProductButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Добавить товар"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@+id/titleTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Кнопка для редактирования товара -->
    <Button
        android:id="@+id/ProductsButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Товары"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/addProductButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Кнопка для отслеживания заказов -->
    <Button
        android:id="@+id/trackOrdersButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Отслеживание заказов"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/ProductsButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Кнопка для выбора склада -->
    <Button
        android:id="@+id/selectWarehouseButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Выбор склада"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/trackOrdersButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Кнопка для финансов -->
    <Button
        android:id="@+id/financesButton"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Финансы"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/selectWarehouseButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>